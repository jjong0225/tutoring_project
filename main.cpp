#include<iostream>
#include<string>

using namespace std;
int main()
{
    while(1)
    {
        string command;
        cin >> command;
        if(command == "4")
            break;
        else
        {
            // do something
        }
    }
    return 0;
}